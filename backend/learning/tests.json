{
  "tests": [
    {
      "name": "Get courses without auth",
      "method": "GET",
      "path": "/?resource=courses",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "Unauthorized"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Get trainers without auth",
      "method": "GET",
      "path": "/?resource=trainers",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "Unauthorized"
      },
      "bodyMatcher": "partial"
    }
  ]
}
